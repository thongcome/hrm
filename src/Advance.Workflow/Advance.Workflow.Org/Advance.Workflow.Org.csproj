<Project Sdk="Microsoft.NET.Sdk">

  <!-- Phase 0 scaffold (see ../EXTRACTION-PLAN.md). Workflow.Org: the
       people/org-chart component the CEO asked Advance.Workflow to own for
       itself (12 ก.ย. 2569), so a standalone sale doesn't need HumanOk. -->
  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <RootNamespace>Advance.Workflow.Org</RootNamespace>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.EntityFrameworkCore" Version="10.0.0" />
    <PackageReference Include="Microsoft.EntityFrameworkCore.SqlServer" Version="10.0.0" />
  </ItemGroup>

  <ItemGroup>
    <!-- IOrgDirectorySource/OrgNode/WorkflowEmployee — a future sync job here
         will map HRM's push (via that interface) onto wf_org_type/wf_employee
         rows. Not implemented yet in Phase 0; just wired for the reference. -->
    <ProjectReference Include="..\Advance.Workflow.Contracts\Advance.Workflow.Contracts.csproj" />
  </ItemGroup>

</Project>
