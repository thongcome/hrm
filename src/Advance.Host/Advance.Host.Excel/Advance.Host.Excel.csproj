<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <RootNamespace>Advance.Host.Excel</RootNamespace>
  </PropertyGroup>

  <ItemGroup>
    <!-- Same version HRM.csproj already pins (see Services/Reporting/Export/ExcelReportExporter.cs
         for the existing ClosedXML usage this helper is modeled to sit
         alongside, not duplicate). -->
    <PackageReference Include="ClosedXML" Version="0.105.1" />
  </ItemGroup>

</Project>
