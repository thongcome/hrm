<Project Sdk="Microsoft.NET.Sdk">

  <!--
    Phase 0 status (see EXTRACTION-PLAN.md "Engine compile status" for the authoritative,
    file-by-file list): PayrollCalculationService, PayrollAnomalyDetectionService,
    PayDateService and HrucfsecurityRateProvider are FULLY adapted to Contracts +
    PayrollDbContext and expected to compile clean. The remaining files (BankFileExportService,
    GLExportService, Por1DataService, WithholdingCertificateDataService,
    PayslipGenerationService, EFilingExportService, PayrollWorkflowService,
    PayrollPreflightService, plus small helpers) are ported with namespaces fixed and
    TODO(seam) comments at every point that used to read Hremployee/other-module tables
    directly, but are NOT required to compile yet per the task brief — getting 100% of
    this project green is explicitly deferred past Phase 0.
  -->

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <RootNamespace>Advance.Payroll.Engine</RootNamespace>
    <AssemblyName>Advance.Payroll.Engine</AssemblyName>
  </PropertyGroup>

  <!-- Phase 0 status: this project does NOT compile clean yet (see header comment above
       and EXTRACTION-PLAN.md "Engine compile status") — several files still reference
       HRM-only types (Hremployee, Kptempreceivedet, HrwOt, Hrd_LifecycleTaskInstance) at
       points marked TODO(seam) that a later pass wires through Advance.Payroll.Contracts.
       The task brief explicitly does not require 100% compile in Phase 0; a full build
       is exercised again once every seam below is closed. -->

  <ItemGroup>
    <PackageReference Include="Microsoft.EntityFrameworkCore" Version="10.0.0" />
    <PackageReference Include="QuestPDF" Version="2026.7.2" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\Advance.Payroll.Core\Advance.Payroll.Core.csproj" />
    <ProjectReference Include="..\Advance.Payroll.Domain\Advance.Payroll.Domain.csproj" />
    <ProjectReference Include="..\Advance.Payroll.Data\Advance.Payroll.Data.csproj" />
    <ProjectReference Include="..\Advance.Payroll.Contracts\Advance.Payroll.Contracts.csproj" />
  </ItemGroup>

</Project>
