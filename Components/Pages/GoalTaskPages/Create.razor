@page "/goaltasks/create"
@using HRM.Data
@using Microsoft.AspNetCore.Authorization
@using Microsoft.EntityFrameworkCore
@using HRM.Model
@using System.ComponentModel.DataAnnotations
@inject IDbContextFactory<HRM.Data.ApplicationDbContext> DbFactory
@inject NavigationManager NavigationManager
@inject UserService UserService

@attribute [Authorize]


<PageTitle>สร้างเป้าหมายของคุณ/Create your Goal</PageTitle>

<h4 class="text-danger">เพิ่มเป้าหมาย/Goal Setting</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <div class="card border-danger mb-3">
            <div class="card-header bg-info text-white">
                เพิ่มเป้าหมาย/Goal Setting
            </div>
            <div class="card-body">
                <EditForm method="post" Model="GoalTask" OnValidSubmit="AddGoalTask" FormName="create" Enhance>
                    <DataAnnotationsValidator />
                    <div class="mb-3">
                        <label for="wol" class="form-label">
                            <a href="/pdf/การตั้งเป้าหมายที่มีประสิทธิภาพด้วยหลัก.pdf" target="_blank"> แนะนำวิธีการตั้งเป้าหมายที่ดี / Smart  Goal Setting </a>
                        </label>
                         
                    </div>
                    <div class="mb-3">
                        <label for="wol" class="form-label">
                            @((typeof(GoalTask).GetProperty(nameof(GoalTask.wol))?.GetCustomAttributes(typeof(DisplayAttribute), false)
                                .FirstOrDefault() as DisplayAttribute)?.Name ?? "WOL")
                        </label>
                        <InputSelect id="wol" @bind-Value="GoalTask.wol" class="form-control">
                            <option value="">-- Select an option --</option>
                            @foreach (var option in wolOptions)
                            {
                                <option value="@option.code">@option.Name</option>
                            }
                        </InputSelect>
                        <ValidationMessage For="() => GoalTask.wol" class="text-danger" />
                    </div>
                    <div class="mb-3">
                        <label for="name" class="form-label">
                            @((typeof(GoalTask).GetProperty(nameof(GoalTask.Name))?.GetCustomAttributes(typeof(DisplayAttribute), false)
                                .FirstOrDefault() as DisplayAttribute)?.Name ?? "Name")
                        </label>
                        <InputText id="name" @bind-Value="GoalTask.Name" class="form-control" />
                        <ValidationMessage For="() => GoalTask.Name" class="text-danger" />
                    </div>
                    <ValidationSummary class="text-danger" />
                    <div class="mb-3">
                        <label for="startdate" class="form-label">
                            @((typeof(GoalTask).GetProperty(nameof(GoalTask.Startdate))?.GetCustomAttributes(typeof(DisplayAttribute), false)
                                .FirstOrDefault() as DisplayAttribute)?.Name ?? "Start Date")
                        </label>
                        <InputDate id="startdate" @bind-Value="GoalTask.Startdate" class="form-control" @oninput="OnStartDateChanged" />
                    <ValidationMessage For="() => GoalTask.Startdate" class="text-danger" />
                    </div>
                   
           @*          <div class="mb-3">
                        <label for="startdate" class="form-label">Start Date</label>
                        <InputDate id="startdate" Value="@GoalTask.Startdate" class="form-control" @oninput="OnStartDateChanged" />
                        <ValidationMessage For="() => GoalTask.Startdate" class="text-danger" />
                    </div> *@

                    <div class="mb-3">
                        <label for="dayofgoal" class="form-label">
                            @((typeof(GoalTask).GetProperty(nameof(GoalTask.DayofGoal))?.GetCustomAttributes(typeof(DisplayAttribute), false)
                                .FirstOrDefault() as DisplayAttribute)?.Name ?? "กี่วัน")  ควรอยู่ระหว่าง 7-14 วัน
                        </label>
                        <InputNumber id="dayofgoal" @bind-Value="GoalTask.DayofGoal" class="form-control" @oninput="OnDayOfGoalChanged" />

                    <ValidationMessage For="() => GoalTask.DayofGoal" class="text-danger" />
                    </div>

                 

                 @*    <div class="mb-3">
                        <label for="dayofgoal" class="form-label">Day of Goal</label>
                        <InputNumber id="dayofgoal" Value="@GoalTask.DayofGoal" class="form-control" @oninput="OnDayOfGoalChanged" />
                        <ValidationMessage For="() => GoalTask.DayofGoal" class="text-danger" />
                    </div> *@

                    <div class="mb-3">
                        <label for="enddate" class="form-label">End Date</label>
                        <input id="enddate" type="date" value="@GoalTask.EndDate?.ToString("yyyy-MM-dd")" class="form-control" readonly />
                        <ValidationMessage For="() => GoalTask.EndDate" class="text-danger" />
                    </div>


@* 
                    <div class="mb-3">
                        <label for="enddate" class="form-label">End Date</label>
                        <InputDate id="enddate" @bind-Value="GoalTask.EndDate" class="form-control" readonly />


                        <ValidationMessage For="() => GoalTask.EndDate" class="text-danger" />
                    </div> *@

                    <div class="mb-3">
                        <label for="description" class="form-label">
                            @((typeof(GoalTask).GetProperty(nameof(GoalTask.Description))?.GetCustomAttributes(typeof(DisplayAttribute), false)
                                .FirstOrDefault() as DisplayAttribute)?.Name ?? "Description")
                        </label>
                        <InputTextArea id="ThankFully" @bind-Value="GoalTask.Description" class="form-control" />
@*                         <InputText id="description" @bind-Value="GoalTask.Description" class="form-control" />
 *@                        <ValidationMessage For="() => GoalTask.Description" class="text-danger" />
                    </div>
                  
                    <div class="form-group">
                      @*   <label for="coachSearch">Search for Facilitator by Name</label>
                        <input type="text" id="coachSearch"  @bind="searchQuery" class="form-control" placeholder="Search coach name" @oninput="FilterCoaches" />
 *@
                        @if (filteredCoaches.Any())
                        {
                            <label for="coachSelect">ชื่อที่ปรึกษา/Facilitator</label>
                            <InputSelect id="coachSelect" @bind-Value="GoalTask.facililatorID" class="form-control">
                                <option value="">-- Select Coach --</option>
                                @foreach (var coach in filteredCoaches)
                                {
@*                                     <option value="@coach.Userid">@coach.Firstname @coach.Lastname</option>
 *@                                    
                                        <option value="@coach.Id">@coach.Firstname @coach.Lastname</option>

                                }
                            </InputSelect>
                        }
                        else
                        {
                            <p>ไม่พบ โค้ช/No Facilitator found.</p>
                        }
                    </div>


                    <button type="submit" class="btn btn-danger">บันทึก/Create</button>
                </EditForm>
            </div>
        </div>
    </div>
</div>

@* <div>
    <a href="/goaltasks">รายการเป้าหมาย/Goal List</a>
</div> *@



@code {
    private List<ScUser> coaches = new List<ScUser>();
    private List<ScUser> filteredCoaches = new List<ScUser>(); // List of coaches filtered by search
    private string searchQuery = ""; // Search input
    private string selectedCoachId = "";  // The selected coach's ID

    private ApplicationDbContext context = default!;
    // Dispose the DbContext
    public async ValueTask DisposeAsync() => await context.DisposeAsync();

    [SupplyParameterFromForm]
    private GoalTask GoalTask { get; set; } = new();
    private List<WOL> wolOptions = new List<WOL>();

    // protected override void OnInitialized()
    // {
    //     InitializeWolOptions();
    // }

    private void InitializeWolOptions()
    {
        // Load or initialize the WOL options here
        context = DbFactory.CreateDbContext();
        wolOptions = context.WOLs.ToList();
    }
    // private void OnStartDateChanged(ChangeEventArgs e)
    // {
    //     // Check if the value has changed and call the update method
    //     if (DateTime.TryParse(e.Value?.ToString(), out DateTime newStartDate))
    //     {
    //         CheckAndUpdateEndDate(); // Call your method to update the EndDate
    //     }
    // }

    private void OnStartDateChanged(ChangeEventArgs e)
    {
        if (DateTime.TryParse(e.Value?.ToString(), out DateTime newStartDate))
        {
            GoalTask.Startdate = newStartDate;  // Update the GoalTask's Startdate
            CheckAndUpdateEndDate(); // Recalculate EndDate
        }
    }


    // private void OnDayOfGoalChanged(ChangeEventArgs e)
    // {
    //     // Check if the value has changed and call the update method


    //         CheckAndUpdateEndDate(); // Call your method to update the EndDate

    // }


    private void OnDayOfGoalChanged(ChangeEventArgs e)
    {
        if (double.TryParse(e.Value?.ToString(), out double newDayofGoal))
        {
            GoalTask.DayofGoal = newDayofGoal;  // Update the GoalTask's DayofGoal
            CheckAndUpdateEndDate(); // Recalculate EndDate
        }
    }

    private async Task AddGoalTask()
    {
        try
        {
            context = DbFactory.CreateDbContext();
            GoalTask.CalculateEndDate();
            string userid =UserService.GetUserId();
            var currentUser = UserService.GetUserName();

            GoalTask.Modby = currentUser;
            GoalTask.ModDate = DateTime.UtcNow;
            GoalTask.CreateDate = DateTime.UtcNow.Date;
            GoalTask.progress = 0M;
            GoalTask.OwnerID = userid;
            if (!String.IsNullOrEmpty(userid))
            {
                ScUser? scUser = await context.ScUsers.Where(b => b.Id == userid).FirstOrDefaultAsync();
                if(scUser is not null)
                {
                    GoalTask.CreateBy = scUser.Firstname + " " + scUser.Lastname;
                    GoalTask.email = scUser.Email;
                }
                else
                {
                    GoalTask.CreateBy = currentUser;
                    GoalTask.email = UserService.GetUserEmail();
                }
            }

             
           
            // GoalTask.email = UserService.GetUserEmail();
            

            context.GoalTasks.Add(GoalTask);
            await context.SaveChangesAsync();
            NavigationManager.NavigateTo("/goaltasks/Maingoal");

        }catch(Exception e)
        {
            e.Message.ToString();
        }
    }

     

    private void CheckAndUpdateEndDate()
    {
        if (GoalTask.DayofGoal.HasValue && GoalTask.Startdate.HasValue)
        {
            GoalTask.CalculateEndDate(); // Update EndDate based on DayofGoal and Startdate
            StateHasChanged(); // Ensure the UI refreshes
        }
    }

    protected override async Task OnInitializedAsync()
    {
        // Fetch the list of available coaches from the service
        using var context = DbFactory.CreateDbContext();

        coaches = await context.ScUsers.Where(u => u.IsCoach).ToListAsync();

        // coaches = await GoalTaskService.GetAllCoachesAsync();
        filteredCoaches = coaches; // Initialize the filtered list
        GoalTask.DayofGoal = 14;
        GoalTask.CalculateEndDate();
        InitializeWolOptions();

        StateHasChanged(); // Force UI update
    }

    // Method to filter coaches based on search query
    private void FilterCoaches(ChangeEventArgs e)
    {
        if (string.IsNullOrWhiteSpace(searchQuery))
        {
            filteredCoaches = coaches; // Show all coaches if no search query
        }
        else
        {
            filteredCoaches = coaches.Where(c => c.Firstname.Contains(searchQuery, StringComparison.OrdinalIgnoreCase)).ToList();
        }


    }

    public void ThisCalculateEndDate()
    {
        if (GoalTask.DayofGoal.HasValue)
        {
            //EndDate = (Startdate ?? DateTime.Now).AddDays((double)DayofGoal);
            if (GoalTask.DayofGoal >= 7 && GoalTask.DayofGoal <= 14)
            {
                if (GoalTask.Startdate.HasValue)
                {
                    GoalTask.EndDate = GoalTask.Startdate.Value.AddDays((double)GoalTask.DayofGoal);

                }
                else
                {
                    // Handle the case where StartDate is null (assign default value if needed)
                    GoalTask.EndDate = DateTime.Now.AddDays((double)GoalTask.DayofGoal);// or any other default logic
                }
            }
        }
    }


}
